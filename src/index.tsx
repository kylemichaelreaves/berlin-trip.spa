/* @refresh reload */
import { render } from 'solid-js/web'
import BerlinTripPage from './BerlinTripPage'
import './index.css'

const root = document.getElementById('root')

render(() => <BerlinTripPage />, root!)
